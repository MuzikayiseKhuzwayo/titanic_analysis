# Data Directory

This directory contains the data used in the project.

- `raw/`: Immutable, original data files.
- `processed/`: Cleaned, transformed data ready for modeling.

**Note:** Large data files are ignored by git to avoid bloating the repository.
